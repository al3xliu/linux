### part of security

#### deny a service
for ubuntu/debian

* deny a service
```
  ufw deny ssh
```
* allow a service
```
  ufw allow ssh
```

#### listen request

for ubuntu/debian

tcpdump port 22
